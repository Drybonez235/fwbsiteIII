{% extends "base.twig" %}

{% block content %}
{# 1. Hero Section #}
<header class="relative min-h-[500px] flex items-center justify-center bg-gray-900 overflow-hidden">
    {% if theme.events_bg%}
        {# Use size-full per v4.1 rules #}
        <img src="{{ theme.events_bg }}" 
             class="absolute inset-0 size-full object-cover opacity-60" 
             alt="{{ theme.events_bg }}">
    {% endif %}
    <div class="relative text-center px-4">
        {# text-shadow-lg is now native in v4.1 #}
        <h1 class="text-white text-5xl md:text-7xl font-serif text-shadow-sm text-shadow-lg tracking-tight">
            Upcoming events at {{ theme.name }}
        </h1>
    </div>
</header>
<div class="mx-auto max-w-4xl px-4 py-12">

    <section class="flex flex-col gap-12">
        {% for event in theme.events %}
            <div class="flex flex-col overflow-hidden rounded-sm bg-white shadow-sm md:flex-row">
    
    {# Uniform Image Section #}
    {% if event.image %}
        {# We use aspect-video on mobile and aspect-square on desktop to lock the dimensions #}
        <div class="w-full shrink-0 md:w-2/5 aspect-video md:aspect-square"> 
            <img 
                src="{{ event.image }}" 
                alt="{{ event.title }}" 
                {# size-full (v4) ensures the image fills the locked aspect container #}
                class="size-full object-cover"
            >
        </div>
    {% endif %}

    {# Content Section #}
    <div class="flex flex-col justify-center p-8">
        <h2 class="text-2xl/8 font-black uppercase tracking-wide text-gray-900">
            {{ event.title }}
        </h2>
        
        <div class="mt-3 flex flex-col gap-1 border-b-2 border-gray-900 pl-4">
            <div class="flex items-center gap-2">
                <p class="text-base/6 text-gray-500">{{ event.date }}</p>
            </div>
            <div class="flex items-center gap-2">
                <p class="text-base/6 text-gray-500">{{ event.location }}</p>
            </div>
        </div>
        
        {% if event.description %}
            {# line-clamp-4 is a good idea if you want to prevent text from pushing the card too long #}
            <p class="mt-6 text-lg/7 text-gray-900">
                {{ event.description }}
            </p>
        {% endif %}
    </div>
</div>
        {% else %}
            <div class="rounded-sm border-2 border-dashed border-gray-200 py-12 text-center">
                <p class="text-lg/7 text-gray-500">No upcoming events scheduled at this time.</p>
            </div>
        {% endfor %}
    </section>
</div>
{% endblock %}